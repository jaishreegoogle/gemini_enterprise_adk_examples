## .github/workflows

This directory contains GitHub Actions workflow files.

### blank.yml

This file defines a basic CI workflow that runs on push or pull request events to the `main` branch. It includes steps to checkout the repository and run simple echo commands.